import { routes as dashboard } from './dashboard';
import { routes as item } from './item';
import { routes as user } from './user';

export default [ ...dashboard, ...item, ...user ];

//const routes = [ ...dashboard, ...item, ...user ];

